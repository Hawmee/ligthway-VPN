1. Clone the repo and `cd` the folder
2. Build and run with `docker compose`
```bash
docker compose up -d
```
> OR Start
```bash
docker compose start
```
3. Web access `localhost:5000`
4. Stop all
```bash
docker compose stop
```
5. Remove all
```bash
docker compose down
```